rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Chỉ cho phép đọc/ghi dữ liệu khi người dùng đã đăng nhập qua Firebase Authentication
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
